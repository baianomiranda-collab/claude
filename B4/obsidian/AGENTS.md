# AGENTS.md

## Workspace context

This workspace is an Obsidian vault, not a standard source code repository.

- Current visible workspace contents: `.claude/settings.local.json`.
- There is no `.obsidian` folder or code/project files at the root level.
- There are no detected build, test, or package manager scripts.

## Guidance for AI coding agents

- Treat this workspace as a notes/vault workspace.
- Before making code changes, verify whether the user intended to work on another repository.
- If the user asks for project-specific development work, ask for the correct codebase or additional context.
- Do not assume standard repository structure, dependencies, or tooling exist.
